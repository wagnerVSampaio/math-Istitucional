import styled from "styled-components";

export const DivOverall = styled.div`
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    display: flex;
    justify-content: space-between;
    box-shadow: 0 5px 5px rgba(42, 42, 238, 0.322);
`